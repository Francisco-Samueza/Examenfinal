@model List<Clasesexamen.ViewData.ViewDataVehiculo>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FirstOrDefault().DescripcionV)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FirstOrDefault().Estado)
            </th>
            <th>
                <label>Vehiculo</label>
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DescripcionV)
            </td>
            <td>
                @if (item.Estado == 1)
                {
                    <label>Activado</label>
                }
                else
                {
                    <label>Desactivado</label>
                }
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nombrevehiculo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Codvehiculo">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.Codvehiculo">Detalles</a> |
                @if (item.Estado == 1)
                {
                    <a asp-action="Desactivar" asp-route-id="@item.Codvehiculo">Desactivar</a>
                }
                else
                {
                    <a asp-action="Activar" asp-route-id="@item.Codvehiculo">Activar</a>
                }

            </td>
        </tr>
}
    </tbody>
</table>
